<%= form_for(@patient) do |f| %>
  <% if @patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
      <% @patient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
	<div class='field'>
		<%= f.label :hospital %><br />
	  <%= collection_select("hospital",:id, @hospitals, :id, :name) %>
	</div>

	</div>
	This is the form partial
  <div class="field">
    <%= f.label :ward_id %><br />
    <%= f.collection_select(:ward_id,@wards,:id,:name)%> 
  </div>
  <div class="field">
    <%= f.label :admission %><br />
    <%= f.date_select :admission %>
  </div>
  <div class="field">
    <%= f.label :reason %><br />
    <%= f.text_field :reason %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
